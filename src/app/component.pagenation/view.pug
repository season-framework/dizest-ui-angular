.pagenation.text-center.area-round
    button.btn.btn-sm.btn-white(
        (click)="move(Math.floor((current - 1) / 10) * 10 - 9)",
        [ngClass]="(current-1)/10 < 1 ? 'disabled' : ''")
        i.fas.fa-angle-double-left
    
    button.btn.btn-sm.btn-white(
        (click)="move(current - 1)",
        [ngClass]="current == 1 ? 'disabled' : ''")
        i.fas.fa-angle-left
    
    ng-container(*ngFor="let page of list")
        button.btn.btn-sm(
            (click)="move(page)",
            [ngClass]="page == current ? 'btn-blue' : ''") {{ page }}
        
    button.btn.btn-sm.btn-white(
        (click)="move(current+1)",
        [ngClass]="current+1 > end ? 'disabled' : ''")
        i.fas.fa-angle-right
    
    button.btn.btn-sm.btn-white(
        (click)="move(Math.ceil(current / 10) * 10 + 1)",
        [ngClass]="Math.ceil(current / 10) * 10 + 1 > end ? 'disabled' : ''")
        i.fas.fa-angle-double-right